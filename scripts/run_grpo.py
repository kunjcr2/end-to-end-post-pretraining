"""
CLI script to launch GRPO Alignment.

This file exists to provide a command-line interface for running GRPO alignment.
It loads the SFT checkpoint and runs alignment with the reward model.

Usage:
    python scripts/run_grpo.py
    python scripts/run_grpo.py --sft-checkpoint ./path/to/sft --max-steps 500
"""

# TODO: Implement CLI with argparse
# - Parse command line arguments to override config values
# - Initialize configs from config/ module
# - Call alignment function from src.align

pass
